interface:
  display_name: "Org Plan"
  short_description: "Author or execute reviewed Org implementation plans"
  default_prompt: "Use $org-plan to author or execute an Org implementation plan with explicit skills, evidence gates, and review state; continue until complete or genuinely externally blocked."
policy:
  allow_implicit_invocation: true
